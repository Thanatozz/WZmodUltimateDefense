//
// Center spawn mode. The behaviour lives in WaveDefense.js; this file exists so the
// slot gets its own scriptName, which is how the rules script reads the mode.
// See WAVE_SCRIPTS in WaveDefenseCommon.js.
//

include("/multiplay/skirmish/WaveDefense.js");
